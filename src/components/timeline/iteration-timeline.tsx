// This component is no longer used and can be deleted.
// The new timeline is implemented in timeline.tsx.
// Keeping the file to avoid breaking imports if it's referenced elsewhere unexpectedly.

const IterationTimeline = () => {
    return null;
}

export default IterationTimeline;
